<div class='mypage_contents'>
  <div class='username-wrapp'>
    <p class='mypage_username'>
      <%= "#{@user.nickname}"%>
    </p>
  </div>
  <div class='mypage_wrapp'>
    <div class='mypage'>
      <% @illusts.each do |illust| %>
        <%= image_tag(illust.image)%>
      <% end %>
    </div>
  </div>
</div>